import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
// import 'package:fluidify_mobile/services/gemini_service.dart';

void main() {
  setUp(() {
    // Inisialisasi mock atau object sebelum setiap test berjalan
  });

  group('GeminiService Tests', () {
    test('Initial test', () {
      // Tulis test Anda di sini ketika service sudah selesai dibuat
      expect(true, isTrue);
    });
  });
}